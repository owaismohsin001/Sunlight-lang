lib "*std"

-- iniu
mod Strict
    Strict <- !{a}

    fmap ? s@$Strict -> $strict: f: s.a
    seq ? f@$Strict & s@$Strict -> $strict: f.a: s.a
    unit ? r@$Strict -> Strict::strict: s
    bind ? s@$Strict -> f: s.a
    a <- 10
end